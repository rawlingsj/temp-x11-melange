# Generated from https://git.alpinelinux.org/aports/plain/main/libunistring/APKBUILD
package:
  name: libunistring
  version: "1.0"
  epoch: 0
  description: Library for manipulating Unicode strings and C strings
  target-architecture:
    - all
  copyright:
    - paths:
        - "*"
      attestation: TODO
      license: GPL-2.0-or-later OR LGPL-3.0-or-later
environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/bootstrap/stage3
      - https://packages.wolfi.dev/os
      - /work/packages
    keyring:
      - https://packages.wolfi.dev/bootstrap/stage3/wolfi-signing.rsa.pub
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - ca-certificates-bundle
      - build-base
      - automake
      - autoconf
pipeline:
  - uses: fetch
    with:
      expected-sha256: 5bab55b49f75d77ed26b257997e919b693f29fd4a1bc22e0e6e024c246c72741
      uri: https://ftp.gnu.org/gnu/libunistring/libunistring-${{package.version}}.tar.xz
  - uses: autoconf/configure
  - uses: autoconf/make
  - uses: autoconf/make-install
subpackages:
  # - name: libunistring-static
  #   pipeline:
  #     - uses: split/FIXME
  #   description: libunistring FIXME
  - name: libunistring-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libunistring
    description: libunistring dev
  # - name: libunistring-doc
  #   pipeline:
  #     - uses: split/manpages
  #   description: libunistring manpages
