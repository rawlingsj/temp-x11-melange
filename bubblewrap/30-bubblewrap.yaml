# Generated from https://git.alpinelinux.org/aports/plain/main/bubblewrap/APKBUILD
package:
  name: bubblewrap
  version: 0.6.2
  epoch: 0
  description: Unprivileged sandboxing tool
  target-architecture:
    - all
  copyright:
    - paths:
        - "*"
      attestation: TODO
      license: LGPL-2.0-or-later
environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/bootstrap/stage3
      - https://packages.wolfi.dev/os
      - /work/packages
    keyring:
      - https://packages.wolfi.dev/bootstrap/stage3/wolfi-signing.rsa.pub
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - ca-certificates-bundle
      - build-base
      - automake
      - autoconf
      - bash
      - meson
      - libcap-dev
      - docbook-xsl
pipeline:
  - uses: fetch
    with:
      expected-sha256: f8d21707b492d318380f0a76966e8cfa40c2df283de8bcf1ac51e7a4dcc12e97
      uri: https://github.com/containers/bubblewrap/archive/v${{package.version}}.tar.gz
  - uses: meson/configure
  - uses: meson/compile
  - uses: meson/install
subpackages:
  - name: bubblewrap-doc
    pipeline:
      - uses: split/manpages
    description: bubblewrap manpages
  # - name: bubblewrap-bash-completion
  #   pipeline:
  #     - runs: FIXME
  # - name: bubblewrap-zsh-completion
  #   pipeline:
  #     - runs: FIXME
