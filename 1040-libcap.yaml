# Generated from https://git.alpinelinux.org/aports/plain/main/libcap/APKBUILD
package:
  name: libcap
  version: "2.66"
  description: POSIX 1003.1e capabilities
  target-architecture:
    - all
  copyright:
    - paths:
        - "*"
      attestation: TODO
      license: BSD-3-Clause OR GPL-2.0-only
environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/bootstrap/stage3
      - https://packages.wolfi.dev/os
      - /work/packages
    keyring:
      - https://packages.wolfi.dev/bootstrap/stage3/wolfi-signing.rsa.pub
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - ca-certificates-bundle
      - build-base
      - automake
      - autoconf
      - perl
      - bash

pipeline:
  - uses: fetch
    with:
      expected-sha256: 15c40ededb3003d70a283fe587a36b7d19c8b3b554e33f86129c059a4bb466b2
      uri: https://kernel.org/pub/linux/libs/security/linux-privs/libcap2/libcap-${{package.version}}.tar.xz
  - uses: autoconf/make
    with:
      opts: GOLANG=no
  - uses: autoconf/make-install
    with:
      opts: GOLANG=no
subpackages:
  - name: libcap-doc
    pipeline:
      - uses: split/manpages
    description: libcap manpages
  # - name: libcap-static
  #   pipeline:
  #     - uses: split/FIXME
  #   description: libcap FIXME
  - name: libcap-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libcap
        - linux-headers
    description: libcap dev
  # - name: libcap-utils
  #   pipeline:
  #     - uses: split/FIXME
  #   description: libcap FIXME
  # - name: libcap2
  #   pipeline:
  #     - runs: FIXME
