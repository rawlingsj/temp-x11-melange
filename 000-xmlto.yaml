# Generated from https://git.alpinelinux.org/aports/plain/main/xmlto/APKBUILD
package:
  name: xmlto
  version: 0.0.28
  epoch: 0
  description: Convert xml to many other formats
  target-architecture:
    - all
  copyright:
    - paths:
        - "*"
      attestation: TODO
      license: GPL-2.0-or-later
environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/bootstrap/stage3
      - https://packages.wolfi.dev/os
      - /work/packages
    keyring:
      - https://packages.wolfi.dev/bootstrap/stage3/wolfi-signing.rsa.pub
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - ca-certificates-bundle
      - build-base
      - automake
      - autoconf
      - bash
      # - libxslt # TODO why was this here?!?
      # - perl-yaml-syck
      # - perl-test-pod
      # - docbook-xsl

pipeline:
  - uses: fetch
    with:
      expected-sha256: 1130df3a7957eb9f6f0d29e4aa1c75732a7dfb6d639be013859b5c7ec5421276
      uri: https://releases.pagure.org/xmlto/xmlto-${{package.version}}.tar.bz2
  - uses: autoconf/configure
  - uses: autoconf/make
  - uses: autoconf/make-install
  - uses: strip
subpackages:
  - name: xmlto-doc
    pipeline:
      - uses: split/manpages
    description: xmlto manpages
