# Generated from https://git.alpinelinux.org/aports/plain/main/cups/APKBUILD
package:
    name: cups
    version: 2.4.2
    description: The CUPS Printing System
    target-architecture:
        - all
    copyright:
        - paths:
            - '*'
          attestation: TODO
          license: GPL-2.0-only
environment:
    contents:
        repositories:
            - https://packages.wolfi.dev/bootstrap/stage3
            - https://packages.wolfi.dev/os
            - /work/packages
        keyring:
            - https://packages.wolfi.dev/bootstrap/stage3/wolfi-signing.rsa.pub
            - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
        packages:
            - busybox
            - ca-certificates-bundle
            - build-base
            - automake
            - autoconf
            - libgcrypt-dev
            - gnutls-dev
            - zlib-dev
            - libpaper-dev
            - dbus-dev
            - libjpeg-turbo-dev
            - avahi-dev
            - linux-headers
            - libusb-dev
            - libgcrypt-dev
            - gnutls-dev
            - zlib-dev
pipeline:
    - uses: fetch
      with:
        expected-sha256: 7095b2977bb728ded5566a5c802866062840d6541fd027836865949a407c3682
        uri: https://github.com/OpenPrinting/cups/archive/v${{package.version}}.tar.gz
    - uses: autoconf/configure
    - uses: autoconf/make
    - uses: autoconf/make-install
subpackages:
    - name: cups-dbg
      pipeline:
        - uses: split/FIXME
      description: cups FIXME
    - name: cups-dev
      pipeline:
        - uses: split/dev
      dependencies:
        runtime:
            - cups
            - libgcrypt-dev
            - gnutls-dev
            - zlib-dev
      description: cups dev
    - name: cups-doc
      pipeline:
        - uses: split/manpages
      description: cups manpages
    - name: cups-libs
      pipeline:
        - uses: split/FIXME
      description: cups FIXME
    - name: ipptool
      pipeline:
        - runs: FIXME
    - name: cups-client
      pipeline:
        - uses: split/FIXME
      description: cups FIXME
    - name: cups-lang
      pipeline:
        - uses: split/FIXME
      description: cups FIXME
    - name: cups-openrc
      pipeline:
        - uses: split/FIXME
      description: cups FIXME
