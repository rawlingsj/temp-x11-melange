# Generated from https://git.alpinelinux.org/aports/plain/main/bash/APKBUILD
package:
    name: bash
    version: 5.2.2
    description: The GNU Bourne Again shell
    target-architecture:
        - all
    copyright:
        - paths:
            - '*'
          attestation: TODO
          license: GPL-3.0-or-later
environment:
    contents:
        repositories:
            - https://packages.wolfi.dev/bootstrap/stage3
            - https://packages.wolfi.dev/os
            - /work/packages
        keyring:
            - https://packages.wolfi.dev/bootstrap/stage3/wolfi-signing.rsa.pub
            - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
        packages:
            - busybox
            - ca-certificates-bundle
            - build-base
            - automake
            - autoconf
pipeline:
    - uses: fetch
      with:
        expected-sha256: a139c166df7ff4471c5e0733051642ee5556c1cc8a4a78f145583c5c81ab32fb
        uri: https://ftp.gnu.org/gnu/bash/bash-5.2.tar.gz
    - uses: autoconf/configure
    - uses: autoconf/make
    - uses: autoconf/make-install
subpackages:
    - name: bash-dbg
      pipeline:
        - uses: split/FIXME
      description: bash FIXME
    - name: bash-dev
      pipeline:
        - uses: split/dev
      dependencies:
        runtime:
            - bash
      description: bash dev
    - name: bash-doc
      pipeline:
        - uses: split/manpages
      description: bash manpages
