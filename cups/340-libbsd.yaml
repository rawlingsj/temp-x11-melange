# Generated from https://git.alpinelinux.org/aports/plain/main/libbsd/APKBUILD
package:
    name: libbsd
    version: 0.11.7
    description: commonly-used BSD functions not implemented by all libcs
    target-architecture:
        - all
    copyright:
        - paths:
            - '*'
          attestation: TODO
          license: BSD-3-Clause
environment:
    contents:
        repositories:
            - https://packages.wolfi.dev/bootstrap/stage3
            - https://packages.wolfi.dev/os
            - /work/packages
        keyring:
            - https://packages.wolfi.dev/bootstrap/stage3/wolfi-signing.rsa.pub
            - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
        packages:
            - busybox
            - ca-certificates-bundle
            - build-base
            - automake
            - autoconf
            - bsd-compat-headers-dev
            - linux-headers-dev
            - libmd-dev
            - libbsd-dev
pipeline:
    - uses: fetch
      with:
        expected-sha256: 9baa186059ebbf25c06308e9f991fda31f7183c0f24931826d83aa6abd8a0261
        uri: https://libbsd.freedesktop.org/releases/libbsd-${{package.version}}.tar.xz
    - uses: autoconf/configure
    - uses: autoconf/make
    - uses: autoconf/make-install
subpackages:
    - name: libbsd-static
      pipeline:
        - uses: split/FIXME
      description: libbsd FIXME
    - name: libbsd-dev
      pipeline:
        - uses: split/dev
      dependencies:
        runtime:
            - libbsd
            - bsd-compat-headers
            - linux-headers
            - libmd-dev
            - libbsd
      description: libbsd dev
    - name: libbsd-doc
      pipeline:
        - uses: split/manpages
      description: libbsd manpages
