# Generated from https://git.alpinelinux.org/aports/plain/main/meson/APKBUILD
package:
  name: meson
  version: 0.63.3
  epoch: 0
  description: Fast and user friendly build system
  target-architecture:
    - noarch
  copyright:
    - paths:
        - '*'
      attestation: TODO
      license: Apache-2.0
  dependencies:
    runtime:
      - python3
      - samurai
environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/bootstrap/stage3
      - https://packages.wolfi.dev/os
      - /work/packages
    keyring:
      - https://packages.wolfi.dev/bootstrap/stage3/wolfi-signing.rsa.pub
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - ca-certificates-bundle
      - build-base
      - automake
      - autoconf
      - py3-setuptools
      - python3
      - samurai
pipeline:
  - uses: fetch
    with:
      expected-sha256: 519c0932e1a8b208741f0fdce90aa5c0b528dd297cf337009bf63539846ac056
      uri: https://github.com/mesonbuild/meson/releases/download/${{package.version}}/meson-${{package.version}}.tar.gz
  - runs: python3 setup.py build
  - runs: python3 setup.py install --prefix=/usr --root=${{targets.destdir}}
  - uses: strip

subpackages:
  - name: meson-doc
    pipeline:
      - uses: split/manpages
    description: meson manpages

